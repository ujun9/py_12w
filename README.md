# py_12w

## 32
- 두 개의 set을 각각 숫자 5개로 만든 후, 합집합과 교집합 계산 후 출력
- `|`, `&` 사용

## 33
- 두 개의 set을 각각 숫자 5개로 만든 후, 차집합과 대칭차집합을 계산 후 출력 
- `-`, `^` 사용

## 34
- 숫자 5개로 set을 만든 후, update method를 이용하여 숫자 100 할당 후 출력
- `|=` 사용

## 35
- a = {100, 200, 300, 400, 500}을 선언 후, {400, 500, 600, 700, 800} set과
  intersection_update method, difference_update method, symmetric_difference_update method 을 수행 후 출력
- `intersection_update`, `difference_update`, `symmetric_difference_update` 사용

## 36
- a = {100, 200, 300, 400, 500}와 {100, 200, 300, 400, 500} set이 있을 때 a가 상위집합이면 "상위", 부분집합이면 "부분", 둘 다면 "동시"를 출력
- `==(동시)`, `>=(상위)`, `<=(부분)` 사용

## 37
- 숫자 5개로 set을 만든 후, 1000 숫자를 add method를 이용해 추가하고, 마지막 값을 삭제 후 출력
- `add()`, `remove()` 사용

## 38
- 아래 코드를 완성하여, 1부터 100까지 숫자 중 3과 5의 공배수를 set comprehension을 이용하여 세트 형태로 출력
